package com.provision.demo.repository;

import com.provision.demo.model.entity.ZoneValue;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ZoneValueRepository extends JpaRepository<ZoneValue, Long> {
}
